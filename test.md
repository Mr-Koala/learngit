``` SQL
mysql> select max(population) from city;
+-----------------+
| max(population) |
+-----------------+
|        10500000 |
+-----------------+
1 row in set (0.00 sec)

mysql> select max(name) from city;
+-------------------------------+
| max(name)                     |
+-------------------------------+
| [San Crist??bal de] la Laguna |
+-------------------------------+
1 row in set (0.00 sec)
```
